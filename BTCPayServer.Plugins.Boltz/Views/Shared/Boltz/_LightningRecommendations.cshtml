@model Google.Protobuf.Collections.RepeatedField<Autoswaprpc.LightningRecommendation>
@if (Model.Count > 0)
{
    <table class="table table-hover my-0">
        <thead>
        <tr>
            <th class="text-nowrap">Channel</th>
            <th>Capacity</th>
            <th>Inbound</th>
            <th>Outbound</th>
            <th>Amount</th>
            <th class="text-nowrap">Type</th>
            <th>Fee Estimate</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var payment in Model)
        {
            <tr>
                <td>@(payment.Channel.Id?.Cln ?? "Total")</td>
                <td>@payment.Channel.Capacity</td>
                <td>@payment.Channel.InboundSat</td>
                <td>@payment.Channel.OutboundSat</td>
                @if (payment.Swap is not null)
                {
                    <td>@payment.Swap.Amount</td>
                    <td>@payment.Swap.Type</td>
                    <td>@payment.Swap.FeeEstimate</td>
                    <td>@payment.Swap.DismissedReasons</td>
                }
                else
                {
                    <td colspan="3" class="text-center">No swap recommended</td>
                }
            </tr>
        }
        </tbody>
    </table>
}